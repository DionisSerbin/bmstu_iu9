public class KeyToken extends Token {

    public KeyToken(DomainTag tag, String image, Position starting, Position following) {
        super(tag, image, starting, following);
    }

    @Override
    public String toString() {
        return super.toString();
    }
}
